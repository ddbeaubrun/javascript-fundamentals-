// Equality: two equalities we can use

    // Equal Operator
    '3' == 3; // will return a true boolean 3 == 3; same

    // Strict equality operator 
    '3' === 3; // will return a false boolean because the string is not equal to the number

    // Opposite operator 

        '3' != 3; // ! is also know as the bang operator; not equal; will return false the opposite of the solution 
        
        '3' !== 3; // will return true boolean

    // Greater than
    3 > 4; // -> false 

    // Less than
    3 < 4; // -> true

    // Greater than or equal to
    3 >= 4; // -> false 

    // Less than or equal to 
    3 <= 4; // -> true

    // AND operator 
    let x = 13;
    x >= 5 && x <= 33;


    // OR operator 
    x > 3 || x == -3

    <= 8, !5
    let y = 1
    y <= 8 && y !== 5 // a number less than or equal to another number, but not a particular number